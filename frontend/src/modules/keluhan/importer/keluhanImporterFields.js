import model from 'modules/satuan/satuanModel';

const { fields } = model;

export default [
  fields.keluhan,
  fields.analisa,
  fields.foto,
  fields.tindakan,
  fields.pemeriksaan,
  fields.keterangan,
];
